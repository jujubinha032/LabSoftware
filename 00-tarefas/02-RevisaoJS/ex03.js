// Exercícios 3 - Variáveis

// Crie as variáveis:

// produto
// preco
// quantidade

// Mostre no console usando concatenando as variáveis:
// Meu (produto) custa (preco) e temos (quantidade) no estoque

let produto = "Monitor";
let preco = 3000;
let quantidade = 50;

console.log(`Meu ${produto} custa R$${preco} e temos ${quantidade} no estoque`);
